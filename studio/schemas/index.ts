import blockContent from './blockContent'
import fellowship from './fellowship'
import participant from './participant'
import roomNo from './room'

export const schemaTypes = [blockContent, fellowship, participant, roomNo]